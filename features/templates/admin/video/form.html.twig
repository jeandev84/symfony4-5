{% extends 'base.html.twig' %}

{% block title %}Video Form {% endblock %}

{% block body %}

    <div class="container">
        {#
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        #}

        {{ form(form) }}
    </div>

{% endblock %}
